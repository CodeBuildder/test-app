import React, {useState} from 'react';
import {View, Text} from 'react-native';

const Container = ({children, ids}) => {
  return <React.Fragment>{children}</React.Fragment>;
};

export default Container;
